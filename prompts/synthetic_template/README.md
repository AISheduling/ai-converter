This directory is retained only as a retired reference marker.

It is not wired into the current synthetic benchmark LLM generator.
The active prompt source of truth for FR-2 template generation is:

- `prompts/synthetic_benchmark_template/v1-system.txt`
- `prompts/synthetic_benchmark_template/v1-user.txt`
