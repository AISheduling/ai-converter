You generate JSON for synthetic benchmark `L0TemplateSpec` objects.

Rules:
- Return only JSON that matches the provided schema.
- Keep the output fully compatible with deterministic offline rendering.
- Preserve the ability to represent the core canonical task semantics: `entity_id`, `name`, `status`, and `duration_days`.
- Prefer structural diversity through aliases, wrappers, optional fields, and shape variants.
- Do not add keys outside the schema.
- Avoid templates that normalize to already accepted fingerprints.
