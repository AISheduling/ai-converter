{
  "experiment_name": "synthetic_schedule_benchmark",
  "run_count": 3,
  "subjects": [
    {
      "name": "compiled_converter",
      "kind": "compiled",
      "notes": "Wrap a deterministic ConverterPackage with BenchmarkSubject.from_converter_package(...)."
    },
    {
      "name": "baseline_converter",
      "kind": "baseline",
      "notes": "Wrap a deterministic callable with BenchmarkSubject.from_converter(...)."
    }
  ],
  "scenarios": [
    {
      "name": "synthetic-base",
      "tags": [
        "synthetic",
        "base"
      ],
      "required_fields": [
        "tasks"
      ],
      "bundle_hint": "tests/fixtures/synthetic_benchmark/bundles/",
      "report_stem": "synthetic_base"
    },
    {
      "name": "synthetic-drift-rename",
      "tags": [
        "synthetic",
        "drift",
        "severity:low",
        "compatibility:rename_compatible"
      ],
      "required_fields": [
        "tasks"
      ],
      "drift_fixture_hint": "tests/fixtures/synthetic_benchmark/drift/rename_status_spec.json",
      "report_stem": "synthetic_drift_rename"
    }
  ],
  "artifacts": {
    "single_run": [
      "benchmark.json",
      "benchmark.csv",
      "benchmark.md",
      "benchmark.telemetry.json"
    ],
    "experiment": [
      "benchmark.experiment.json",
      "benchmark.experiment.md",
      "benchmark.summary.json",
      "benchmark.summary.csv",
      "benchmark.boxplot.csv",
      "benchmark.telemetry.summary.json",
      "benchmark.telemetry.summary.csv",
      "benchmark.telemetry.boxplot.csv",
      "runs/<run_id>/benchmark.json",
      "runs/<run_id>/benchmark.telemetry.json"
    ]
  },
  "runner_example": "examples/synthetic_benchmark/run_example.py",
  "notes": [
    "This file is illustrative only; the benchmark layer stays library-first.",
    "Repeated runs should reuse run_repeated_benchmark(...) instead of introducing a second harness.",
    "Keep benchmark subjects, scenarios, and assertions offline and deterministic.",
    "Use benchmark.boxplot.csv for canonical repeated-run metric distributions and benchmark.telemetry.boxplot.csv for timing distributions.",
    "examples/synthetic_benchmark/run_example.py is the thin runnable example for this workflow."
  ]
}
