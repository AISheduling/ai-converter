Synthetic benchmark bundle fixtures live here when a test needs a repo-local
sample directory. The focused TASK-Bench-01 unit suite mostly uses `tmp_path`
round-trips and keeps this directory as the stable fixture root for future
bundle examples.
